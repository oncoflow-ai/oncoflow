# OncoFlow inference algorithm - Python dependencies
#
# Additive to ml/exploration/requirements.txt; no version downgrades.

# Core numerics / IO
numpy>=1.26
scipy>=1.11
nibabel>=5.1
SimpleITK>=2.3
pydantic>=2

# Image registration
antspyx>=0.4

# Deep learning backends
torch>=2.2
transformers>=4.47
accelerate>=0.27

# nnU-Net v2 (local + CLI backends)
nnunetv2>=2.4

# MONAI fallback bundle + optional skull-strip
monai>=1.3
antspynet                  # CPU skull-strip fallback (MorphAnt)
# hd-bet ; platform_system != "Darwin"   # optional GPU skull-strip

# SAM family - install manually as needed (weights not on PyPI):
#   pip install git+https://github.com/facebookresearch/sam2.git
#   pip install git+https://github.com/facebookresearch/sam3.git   # when public
#   pip install medsam                                             # optional

# CLI + config
typer>=0.12
PyYAML>=6.0

# Plotting (optional; benchmark harness uses it if available)
matplotlib>=3.8

# Testing
pytest>=8.0

# Google Cloud (Vertex AI client)
google-auth>=2.20
requests>=2.30
google-cloud-aiplatform>=1.35
